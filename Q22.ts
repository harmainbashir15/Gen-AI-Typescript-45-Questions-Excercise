let arr = [1, 2, 3];
console.log(arr[3]); // This will cause an index error because the array only has 3 elements
let array = [1, 2, 3];
console.log(arr[2]); // This will output 3, which is the element at index 2

// Or, you can add more elements to the array
arr[3] = 4;
console.log(arr[3]); // This will output 4, which is the element at index 3