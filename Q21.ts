interface Person {
    name: string;
    age: number;
  }
  
  const person: Person = {
    name: 'Harmain',
    age: 19
  };
  
  console.log(`Name: ${person.name}`);
  console.log(`Age: ${person.age}`);